API Service for ordering goods for retail
====

Install packages:
```bash
pip install -r requirements.txt
```

Make migrations:
```base
./manage.py migrate
```

Start server:
```base
./manage.py runserver
```
